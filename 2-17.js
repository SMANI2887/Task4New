// Given their radius of a circle and find its diameter, circumference and area.

let Radius=6;
let diameter=0;
let circumferece=0;
let area=0;
diameter = 2*Radius;//2*R
circumferece=2*Math.PI*Radius;//c=2*PI*r
area=Math.PI*Math.sqrt(Radius);
console.log(diameter);
console.log(circumferece);
console.log(area);